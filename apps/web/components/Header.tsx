"use client";

import RandomBtn from "@repo/ui/src/RandomBtn";

const Header = () => {
	return (
		<header>
			<nav onClick={() => {}}>
				<RandomBtn loading={false} />
			</nav>
		</header>
	);
};

export default Header;
